#include "main.h"

/**
 * _abs - computes the absoulte value of a number
 * @t: the number to be computed
 * Return: the absoulte value
 */

int _abs(int t)
{
	if (t > 0)
		return (t);
	else
		return (-t);
}
